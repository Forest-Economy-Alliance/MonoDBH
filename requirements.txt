# ──────────────────────────────────────────────────────────────────────────────
# MonoDBH – Python runtime dependencies
# ──────────────────────────────────────────────────────────────────────────────
#
# FULL PIPELINE (grounded_sam2_base.py / grounded_sam2_florence2.py / sam2_segmentation.py)
# ─────────────────────────────────────────────────────────────────────────────
# PyTorch must be installed FIRST with the correct CUDA index URL, then install
# the rest of the dependencies:
#
#   # GPU (CUDA 12.1)
#   pip install torch>=2.3.1 torchvision>=0.18.1 torchaudio \
#       --index-url https://download.pytorch.org/whl/cu121
#
#   # CPU-only
#   pip install torch>=2.3.1 torchvision>=0.18.1 torchaudio
#
#   pip install -r requirements.txtAUTHOR = "[Ashwini Chhatre, Abhijeet Parmar, Nitesh Saini, Mayank Jain]"
#   pip install -e ".[notebooks]"                            # SAM 2
#   pip install --no-build-isolation -e grounding_dino      # Grounding DINO
#
# DBH ESTIMATOR ONLY (utils/dbh_estimator.py)
# ─────────────────────────────────────────────
# No GPU or PyTorch required. Use a plain venv and install the lightweight
# subset only:
#
#   python -m venv .venv
#   # Linux/macOS: source .venv/bin/activate
#   # Windows:     .venv\Scripts\activate
#   pip install pillow>=9.4.0 pandas>=2.2.0
# ──────────────────────────────────────────────────────────────────────────────

# ── Core ML / vision ──────────────────────────────────────────────────────────
numpy>=1.24.4
tqdm>=4.66.1
hydra-core>=1.3.2
iopath>=0.1.10
pillow>=9.4.0
opencv-python>=4.7.0

# ── Detection & segmentation ──────────────────────────────────────────────────
supervision>=0.21.0
transformers>=4.40.0

# ── Data / analysis ───────────────────────────────────────────────────────────
pandas>=2.2.0

# ── Notebooks / visualisation (optional) ─────────────────────────────────────
matplotlib>=3.9.1
jupyter>=1.0.0
eva-decord>=0.6.1
